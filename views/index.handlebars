<h1> {{title}} </h1>
<ul>
    {{#each recordsToDisplay}}

    {{/each}}
</ul>

<table class="record-table">
    <tr>
        <th>PruID</th>
        <th>PrName</th>
        <th>PrNameFR</th>
        <th>Date</th>
        <th>NumConf</th>
        <th>NumProb</th>
        <th>NumDeaths</th>
        <th>NumTotal</th>
        <th>NumToday</th>
    </tr>

    {{#each recordsToDisplay}} 

    <tr>
        <td> {{this.pruid}} </td>
        <td> {{this.prname}} </td>
        <td> {{this.prnameFR}} </td>
        <td> {{this.date}} </td>
        <td> {{this.numconf}} </td>
        <td> {{this.numprob}} </td>
        <td> {{this.numdeaths}} </td>
        <td> {{this.numtotal}} </td>
        <td> {{this.numtoday}} </td>
    </tr>

    {{/each}}

</table>

